{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for Little V windows",
  "windows": ["main", "ticker"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
